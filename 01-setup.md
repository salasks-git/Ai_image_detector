# Step 1: Environment Setup

This step ensures your machine has all required libraries, including `nest_asyncio` to prevent the common Streamlit runtime threading crash.

## 1. Install Dependencies
Open your terminal and run the following command:

```bash
pip install google-antigravity streamlit pillow nest_asyncio